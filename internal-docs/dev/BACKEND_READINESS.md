# Backend — готовность к деплою «купил сервер, раскатал репу»

Разбор на 24.09.2026. Коротко: **Go-код собирается и логика в целом на месте, но «как есть» завтра не взлетит**.
Мешают конфиг деплоя, расхождение дат между приложением и планировщиком и открытый API, который позволяет сжечь бюджет LLM.

---

## Статус на 24.09 (вечер)

Блокеры 1–6 закрыты и проверены на локальном Postgres 16 с поддельным LLM (полный путь: данные → чек-ин → `/morning` → `/metrics`, ночной проход планировщика, дневной лимит):

- **1** — release APK ходит на `https://api.companion.keegooroomie.ru/` (тот же хост, что в `metrics.html`)
- **2, 3** — `setup.sh`: Caddyfile многострочный (проверен `caddy validate`; старый однострочный Caddy действительно не принимал), пароль БД генерируется, `.env` пишется сам, cron-бэкап `pg_dump` в 03:30 (7 дней), без Docker. `deploy.sh`: миграции с `schema_migrations`, установка systemd-юнита, замена бинарника, проверка `/health`
- **4** — пакет `clock` (`APP_TZ`, по умолчанию Москва) для всех дат; генерация в 04:10 МСК по данным за вчера (+ до 6 дней раньше) для тех, кто присылал данные последние 2 дня; 4 потока, повтор неудачных через 2 мин; `GET /morning` генерирует по запросу, если ночью не вышло (блокировка на пользователя). Android-воркер шлёт вчера целиком + сегодня; upsert не затирает поля пустыми. `LastN` — от старых к новым и время в `HH:MM` (из-за `HH:MM:SS` не работало «проснулся → взял телефон через N мин»)
- **5** — `call_log` пишется с `context.WithoutCancel` + таймаут 5 с
- **6** — `GET /api/v1/metrics`: все ключи `metrics.html` + `dau_history`, кэш 60 с, CORS; DAU/retention по новой таблице `user_activity` (приложение шлёт `POST /api/v1/ping` при открытии; чек-ин и `/morning` тоже считаются), uptime по минутным `heartbeats`, стоимость по токенам и ценам из env
- Плюс: `LLM_DAILY_CAP` (по `morning_messages`, синхронно), таймаут LLM 25 с, лимит тела 64 КБ, `/health` проверяет БД, `component=checkin` / `morning_api` в `call_log`

Осталось: **7** (регистрация с токеном, rate limit), промпты в БД, CI для Go, `weekly_feedback`.

## Блокеры (без них не работает или опасно)

| # | Что | Где | Что будет | Правка |
|---|-----|-----|-----------|--------|
| 1 | В release-сборке APK адрес API — `https://YOUR_DOMAIN/` | `android/app/build.gradle.kts` | приложение никуда не ходит | домен → пересобрать APK. `metrics.html` уже ждёт `api.companion.keegooroomie.ru` |
| 2 | `setup.sh` пишет Caddyfile в одну строку через `;` | `deploy/setup.sh` | в Caddyfile `;` не разделитель → Caddy не стартует | копировать `deploy/Caddyfile` с `sed s/YOUR_DOMAIN/…/` |
| 3 | Пароль БД `CHANGE_ME_IN_ENV` создаётся буквально | `deploy/setup.sh` | надо руками менять и в Postgres, и в `.env` | генерировать пароль в скрипте и сразу писать `.env` |
| 4 | Утренняя генерация почти ни для кого не сработает | `scheduler.go`, `MorningRepo.UsersWithoutMessage`, `DailyCollectWorker` | см. «Цепочка утреннего прогноза» ниже | собирать *вчера целиком* и генерировать по вчерашним данным |
| 5 | `call_log` пишется горутиной с контекстом запроса | `analytics/call_log.go` | после ответа контекст отменён → вставка падает `context canceled` → **логи антифрода теряются** | `context.WithoutCancel(ctx)` + таймаут |
| 6 | Нет `/api/v1/metrics` | `api/router.go` | страница метрик для жюри показывает только мок | эндпоинт вне `UserMiddleware` + CORS для Pages |
| 7 | Любой может выдать себя за любого пользователя и создать сколько угодно пользователей | `UserMiddleware` (`X-User-ID`) | чтение чужих прогнозов; бот шлёт passive data с тысячей id → в 04:10 тысяча вызовов LLM → **бюджет 50 k₽ уходит за ночь** | см. «Безопасность» |

## Цепочка утреннего прогноза — где она рвётся

1. `DailyCollectWorker` раз в 12 ч шлёт снимок **сегодняшнего дня с полуночи до «сейчас»**. После полуночи строка вчерашнего дня больше не обновляется: вечер теряется.
2. Планировщик в 04:10 **по времени сервера** (Ubuntu по умолчанию UTC → 07:10 МСК) берёт пользователей, у которых есть `daily_data` **за сегодня**. Такая строка к этому моменту есть, только если телефон случайно отработал между 00:00 и 07:10.
3. «Сегодня» считается как `time.Now().Truncate(24h)` — это полночь по UTC. С 00:00 до 03:00 МСК это ещё вчерашняя дата.
4. `DailyRepo.LastN` возвращает дни от новых к старым, а `buildPrompt` считает, что от старых к новым → «сон снижается N дней» срабатывает, когда сон **растёт**.
5. Если сообщения нет, `GET /morning` отдаёт 404, и повторной попытки нет. Упал вызов LLM — пользователь до завтра без прогноза.

**Как надо:**
- приложение раз в сутки после ~03:00 отправляет **вчерашний день целиком** (плюс, как сейчас, сегодняшний частичный);
- сервер работает в явном часовом поясе (`TZ=Europe/Moscow`, дата — `time.Now().In(loc)`);
- генерация по данным «вчера + 6 дней до»;
- `GET /morning` при отсутствии сообщения генерирует его синхронно, с блокировкой на пользователя, `trigger=user_action`. Это заодно снимает вопрос Q1 к организаторам: вызов происходит в момент открытия;
- ночной батч оставить как прогрев кэша и повторить для тех, у кого была ошибка.

## Безопасность и бюджет

- **Идентификация.** Сейчас `X-User-ID = SHA256(ANDROID_ID + соль)`, соль лежит в APK. Надо: `POST /api/v1/register` → сервер выдаёт `user_id` + случайный `token`, дальше `Authorization: Bearer`. Храним только хэш токена. Без этого прогноз любого пользователя читается по его id.
- **Бюджет LLM.** Дневной лимит вызовов (`LLM_DAILY_CAP`): при превышении — локальный фолбэк и алерт в TG. Генерировать только для пользователей с данными за вчера **и** с активностью за 2 дня. Не больше одного вызова на пользователя в сутки (уже есть через `UNIQUE(user_id,date)`).
- **Лимиты запросов.** Rate limit по IP на `/register` и по пользователю на остальное (`go-chi/httprate`). `http.MaxBytesReader` на тело (64 КБ). `chi/middleware.Timeout`.
- **LLM-клиент** без таймаута: один зависший вызов стопорит весь ночной батч. `context.WithTimeout(ctx, 20*time.Second)` на вызов, 1–2 повтора с паузой.
- **Выходной текст LLM.** Проверять на ссылки и телефоны, резать по длине (есть).

## Что ещё не хватает (не блокеры)

- `POST /feedback/weekly` — заглушка, ничего не сохраняет
- В `Checkin` пишется `component=usagestats` вместо отдельного `checkin`
- `session_id` в `call_log` всегда пустой — приложение не передаёт (заголовок `X-Session-ID`)
- `/health` не проверяет БД → добавить `db.Ping` (для uptime в метриках)
- Нет бэкапов Postgres → `pg_dump` по cron + хранение 7 дней
- Нет CI для бэка: `go build`, `go vet`, `gofmt` в Actions
- Миграции: в `docker-compose` через `initdb.d` (только при первом старте), в `deploy.sh` — прогон всех `.sql` каждый раз (сейчас идемпотентно, но хрупко). Нужна таблица `schema_migrations` или `golang-migrate`
- `setup.sh` ставит Docker, который путь через systemd не использует
- Сканирование `TIME` → `*string` (`bedtime`, `first_unlock`) в `LastN` не проверено на живой БД. Проверить до деплоя одним прогоном

## Промпты через CRUD — да, но не публичным API

Это удобно: правки тона без деплоя и A/B вариантов. Угроза реальная: утёк админ-токен → кто угодно пишет всем пользователям от имени приложения (ссылки, вредные «советы»).

Схема, которую стоит заложить:

1. Таблица `prompts(id, name, version, body, is_active, created_at, created_by, note)`. Правка создаёт **новую версию**, старые не трогаются, откат — переключить `is_active`.
2. `morning_messages.prompt_version` — видно, каким промптом сделан каждый текст. Это нужно и для метрик, и для A/B.
3. Админ-эндпоинты (`GET/POST /admin/prompts`, `POST /admin/prompts/{id}/activate`, `POST /admin/prompts/preview` — собрать промпт для user_id и, по флагу, вызвать LLM) — **на отдельном порту, слушающем только `127.0.0.1`**. Caddy его не проксирует, доступ только через `ssh -L 9090:localhost:9090 server`. Наружу не торчит вообще.
4. Плюс `ADMIN_TOKEN` (32+ байта, сравнение `subtle.ConstantTimeCompare`) — на случай, если порт когда-то откроют по ошибке.
5. Каждое изменение и активация → запись в `call_log`/аудит и сообщение в Telegram-бот «промпт morning v7 активирован».
6. Фолбэк: если активного промпта в БД нет, берётся встроенный из кода (`go:embed prompts/morning.md`) — сервер не падает на пустой таблице.

Альтернатива попроще до конкурса — промпты файлами в репе (`go:embed`) и правка через коммит + `deploy.sh`. Безопасно и с историей, но нужен деплой на каждую правку.

## Порядок работ

1. Блокеры 2, 3, 5, 4 (цепочка прогноза + часовой пояс + `LastN`), 6 — день работы
2. Регистрация с токеном + лимит бюджета + rate limit — ещё день; приложению нужна небольшая правка под токен
3. Промпты в БД + админ-порт на localhost
4. Бэкапы, CI для Go, миграции
5. Домен → адрес API в release → пересборка APK → нагрузочный тест из `deploy/README.md`
